export type Topics = {
    id: string;
    slug: string;
    title: string;
    cover_photo : {
        urls: {
            thumb: string;
        }
    }
};

export type Search = {
    results: {
        
    }
}